<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <!-- Unique identifier of the plugin. It should be FQN. It cannot be changed between the plugin versions. -->
    <id>pl.semidude.tree-view-by-default</id>

    <!-- Public plugin name should be written in Title Case.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-name -->
    <name>Tree View By Default</name>

    <!-- A displayed Vendor name or Organization ID displayed on the Plugins Page. -->
    <vendor email="radoslaw.panuszewski15@gmail.com">semidude</vendor>

    <!-- Description of the plugin displayed on the Plugin Page and IDE Plugin Manager.
         Simple HTML elements (text formatting, paragraphs, and lists) can be added inside of <![CDATA[ ]]> tag.
         Guidelines: https://plugins.jetbrains.com/docs/marketplace/plugin-overview-page.html#plugin-description -->
    <description>
        The data grid with query results by default is in the 'Table' presentation mode - this plugin changes that default to 'Tree'.

        Additionally, if the results contain only one row, then it is automatically expanded.
    </description>

    <!-- Product and plugin compatibility requirements.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html -->
    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.database</depends>

    <!-- Extension points defined by the plugin.
         Read more: https://plugins.jetbrains.com/docs/intellij/plugin-extension-points.html -->
    <extensions defaultExtensionNs="com.intellij">
        <applicationService
            serviceImplementation="pl.semidude.mongoviewtweaks.TreeViewByDefaultSettingsState" />

        <applicationConfigurable
            parentId="tools"
            instance="pl.semidude.mongoviewtweaks.TreeViewByDefaultSettingsConfigurable"
            id="pl.semidude.mongoviewtweaks.TreeViewByDefaultSettingsConfigurable"
            displayName="TreeViewByDefault Settings" />
    </extensions>

    <actions>
        <action
            id="pl.semidude.mongoviewtweaks.TreeViewByDefaultAction"
            class="pl.semidude.mongoviewtweaks.TreeViewByDefaultAction"
            text="Table View By Default"
            description="The action which sets the TREE view as default for the result grid.">

            <add-to-group group-id="Console.TableResult.Group.Secondary" anchor="first" />

        </action>
    </actions>

</idea-plugin>